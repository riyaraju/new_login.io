<% if user_signed_in? %>
    <ul class="nav">
      <% if user_signed_in? %>
          <% if current_user.role == 'admin' %>
              <li><%= link_to 'Manage User', user_profiles_list_path %></li>
              <li><%= link_to 'Add New User',new_user_profile_path %></li>
                <li><%= link_to 'Edit ME',edit_user_profile_path(current_user.id)%></li>
          <%end%>
      <%end%>
      <li><%= link_to "Logout", destroy_user_session_path, :method => :delete%></li>
      <%= render 'layouts/navigation_links' %>
    </ul>
<%end%>
